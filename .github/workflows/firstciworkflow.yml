name: firstcicd
on: workflow_dispatch
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "hello world
      - name: Print goodbye
        run: echo "goodbye world"
    
